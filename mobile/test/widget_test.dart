// Placeholder widget test — full tests are in test/features/
// The app requires dependency injection setup which is not done in widget tests.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
