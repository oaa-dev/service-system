services:
  nextjs-customer:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app               # your project code
      - /app/node_modules     # keep node_modules inside the container
    command: sh -c "npm install && npm run dev"
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: development
